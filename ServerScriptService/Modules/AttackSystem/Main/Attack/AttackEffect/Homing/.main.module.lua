local Stats = { };

function effect (character)
	-- nothing here
end

function spawned ()
	-- calculate a target
	-- start homing towards that target
end

Stats.effect = effect;
Stats.onSpawn = spawned;

return Stats;